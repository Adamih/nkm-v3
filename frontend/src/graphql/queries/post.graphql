query Post($id: Int!) {
  post(id: $id) {
    id
    createdAt
    updatedAt
    title
    text
    points
    creator {
      id
      firstName
      lastName
      email
    }
    voteStatus
  }
}
